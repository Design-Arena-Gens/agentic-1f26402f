# Toyota Logo Downloader

A minimal Next.js app that serves a locally stored Toyota logo SVG with a download button.

## Development

- Install: `npm install`
- Build: `npm run build`
- Start: `npm start`

## Deploy

Deploy to Vercel:

```bash
vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-1f26402f
```
